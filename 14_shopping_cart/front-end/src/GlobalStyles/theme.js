import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#14213d',
    },
    secondary: {
      main: '#F5793A',
    },
    background: {
      default: '#FDFAE9',
    },
  },
})

export default theme
